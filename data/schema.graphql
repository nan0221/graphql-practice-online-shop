type Query {
  products: [Product!]!
  shoppingCart(userId: ID!): [Product!]!
}

type Mutation {
  addProduct(name: String!): Product
  editProduct(productId: ID!, details: Object): Product
  addCustomer(email: String!, password: String!): Customer
}

# type User {
#   id: ID!
#   email: String
#   name: String!
#   posts: [Post!]!
# }

# type Post {
#   id: ID!
#   title: String!
#   published: Boolean!
#   author: User
# }


type Product {
  id: ID! @unique
  name: String
  desc: String
  photo: String
  Price: String
}


type Customer {
  id: ID! @unique
  email: String! @unique
  firstName: String
  lastName: String
  address: String
  phone: String
  password: String!
  # TODO encryption?
  productsByCustomer: [Product!]!
}

type Admin {
  id: ID! @unique
  username: String! @unique
  password: String!
  # TODO encryption?
}